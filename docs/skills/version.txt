name: xiaoya-ai-cinema-travel
version=1.2.1
description: 小鸭AI旅游短视频提示词 — 旅游行业AI短视频制作专家，支持智能分镜可视化
release_date: 2026-08-20
changelog: |
  v1.2.1 — 视频提示词改三轨式（视听描述/声音氛围/配乐），多镜头用 [Shot N] At MM:SS.mmm 时间戳锚定；新增参考图视觉描述优先原则（读图能力自动生成中文视觉描述）
  v1.2.0 — 镜头格式迁移至官方 Begin with Shot N [X-Ys]（Then Shot 平滑过渡 / Cut to Shot 仅场景切换），废弃 S1: 与「第N个镜头」格式；运镜优先无人机/POV步行/延时摄影，同目的地连续平滑运镜禁止硬切
  v1.1.0 — 旅游全链路调优：补充文化古迹六宫格+风格模板，添加强制写实规则/禁止CG感，补齐景区风光/打卡Vlog/美食探店/文化古迹4种示例，仪表盘参数操作化映射指南
  v1.0.1 — 移除 @图N 参考指代语法：用户自行上传参考图，SKILL不再输出 @图N 标注
  v1.0.0 — 初始版本：旅游行业专属SKILL，酒店宣传片/民宿探店/景区风光/城市打卡/美食探店五大视频类型，15种旅游专属运镜，空间递进叙事结构
  v2.6.58 — 爆款灵感新增合规铁律：灵感创意方案也禁止浴室/洗澡等违禁场景，从源头拦截审核风险
  v2.6.57 — 第四步新增阿里云违禁场景词：禁止"浴室/洗澡/淋浴/卫生间"，强制用场景暗示替代
  v2.6.56 — 强化【分镜映射】规则：加"每段都必须输出"铁律到规则第一条，修复第二段遗漏映射的问题
  v2.6.55 — 【分镜映射】JSON：宫格生成时 AI 同时输出 Shot→Frame 映射，前端解析存入节点缓存，视频生成时注入精确帧引用，实现分镜→宫格→视频三环锁定
  v2.6.54 — 删除"画面内仅包含"尾句声明：参考图+prompt_rule+negative_prompt 三重系统锁已足够，AI 提示词只写运镜+动作+声音
  v2.6.53 — 合并上游 v2.6.52（画面锁定三层防护 + 先观察→再生成两段式 prompt_rule）+ 保留动作时间规划 + 动作密度铁律统一措辞
  v2.6.52 — 已废弃：画面锁定铁律（每个镜头末尾声明物体清单，v2.6.54 移除）
  v2.6.51 — 动作密度铁律：每个镜头最多2-3个动作，复杂动作链拆分到多镜头（对齐 video_gen_rules v21）
  v2.6.50 — 启动流程透明化：版本检查不再阻塞用户消息，💡/视频描述直达工作流
  v2.6.49 — 人脸光影铁律 + 黑块/黑斑负面词：禁止背光/强侧光，negative prompt 新增7项黑块术语
  v2.6.48 — L3 继续确认：前端气泡外按钮 + 【继续确认】标记 + chat.rs 三段式
    - 多段视频每段宫格后追加【继续确认】标记，前端检测后渲染"继续"/"暂停"按钮
    - chat.rs 系统提示词新增"继续"/"暂停"触发词，支持逐段生成
    - parsePromptBlocks 剥离【继续确认】段，存入 ChatMessage.continuationPrompt
    - L2/L3 通过不同消息区分（L2用"A"，L3用"继续"），互不冲突
    v2.6.42 — L2/L3 改为分隔线模式（已废弃）
    v2.6.41 — 气泡隔离铁律（已废弃）
    v2.6.40 — 第八步末尾明确 L3 继续确认触发时机
    v2.6.39 — 灵感搜索改为直接行为指令
    v2.6.38 — 灵感步骤2/3输出边界强化
    v2.6.37 — 多段流程 L2/L3 分离 + 修复五处逻辑矛盾
    v2.6.35 — 爆款灵感时长铁律 + 恢复灵感模式章节
    v2.6.33 — 默认时长修正：5-15秒 → 20-30秒
